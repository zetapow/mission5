
# Example environment variables

# ====== Backend (Node.js/Express) ======

# Application port
PORT=5000

# MongoDB connection string (update with your credentials and cluster)
MONGODB_URI=mongodb://localhost:27017/test

# CORS allowed origins (comma-separated, e.g. frontend dev URL)
CORS_ORIGIN=http://localhost:5173

# ====== Frontend (React) ======

# API base URL (used by frontend to call backend)
REACT_API_URL=http://localhost:4000

# Google Maps API key (if using map integration)

# Node environment (development, production, etc.)
NODE_ENV=development-  [@Adam T.](https://github.com/AdamT-HJ)
